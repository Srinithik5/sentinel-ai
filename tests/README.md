# Tests

Root-level end-to-end and cross-service integration tests spanning `frontend`, `backend`, and `ai-engine`.

Service-level unit tests live alongside each service (e.g. `backend/tests`). This directory is reserved for later phases once integration flows exist to test.