# Shared

Cross-service contracts shared between `frontend`, `backend`, and `ai-engine` — e.g. TypeScript types mirrored from backend Pydantic schemas, shared enums, and constants.

Empty in Phase 1. Populated as the API surface between services stabilizes.